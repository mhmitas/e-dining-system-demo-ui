import React from 'react';
import PunchCard from './PunchCard';

const PunchCardSection = () => {
    return (
        <div>
            <PunchCard />
        </div>
    );
};

export default PunchCardSection;