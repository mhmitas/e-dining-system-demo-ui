import PunchCardSection from '@/components/my-ui/PunchCardSection';
import React from 'react';

const Page = () => {
    return (
        <section className='my-container'>
            <PunchCardSection />
        </section>
    );
};

export default Page;